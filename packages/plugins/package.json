{
  "name": "@digitaplatform/plugins",
  "version": "0.1.0",
  "type": "module",
  "description": "Digita frontend plugin SDK: the host's only import surface — the provideHostServices/useHost DI singleton plus the FrontendPlugin / PluginManifest / Layout / Template contracts. The host (ui) provides the singleton instance; plugin bundles consume it as an import-map singleton at runtime.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/digitaplatform/digita-platform.git",
    "directory": "packages/plugins"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
