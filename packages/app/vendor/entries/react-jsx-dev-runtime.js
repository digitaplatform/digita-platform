// Vendor ESM for react/jsx-dev-runtime. Prod builds (NODE_ENV=production) use the
// non-dev runtime, but the key is mapped so any dev-runtime import resolves to the
// shared react rather than a bundled-in copy with a throwing require shim.
export { Fragment, jsxDEV } from 'react/jsx-dev-runtime';
