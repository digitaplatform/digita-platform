node_modules
dist
dist-ssr
*.local
.vite

# Generated by build:assets (vendor ESM + plugin bundles + import-map).
# Rebuilt by Tekton/CI on every image build — never commit the artifacts.
public/vendor/
public/plugins/
public/import-map.json
public/plugins-premium/
